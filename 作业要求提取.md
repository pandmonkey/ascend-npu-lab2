# 并行与分布式导论 Lab2 作业要求提取

来源：`new-pdc01-intro.pdf`

## 基本信息

- 作业名称：Lab2：昇腾 NPU 微基准测试设计
- 截止日期：2026 年 6 月 20 日 23:59
- 提交方式：将 `lab2_学号_姓名.zip` 提交至教学网
- 实验平台：昇腾 910B 计算卡
- 核心任务：设计并运行 microbenchmark，通过实测获得昇腾计算卡各微架构组件的性能参数，包括延迟、带宽、吞吐率、流水线深度、容量等。

## 实验背景与方法论

本实验围绕昇腾 DaVinci 架构展开，涉及 AI Core、Scalar Unit、Vector Unit、Cube Unit、L1 Buffer、L0A、L0B、L0C、HBM、MTE 等组件。

PDF 要求参考 Accel-Sim GPU Microbenchmark 的方法论：

1. 逐组件隔离测量：每次只让一个硬件单元工作，尽量排除其他单元干扰。
2. 参数化控制：改变数据规模、循环次数等参数，观察性能变化。
3. 从简单推导复杂：先测原子操作延迟，再测流水线深度，最后组合建模。

常用 microbenchmark 设计模式：

- Pointer Chasing：用于测量存储访问延迟，通过构造指针链表和随机访问消除缓存预取影响。
- Streaming：用于测量带宽，发起大量连续数据传输，逐步增大数据量直到带宽饱和。
- Dependency Chain：用于测量运算单元延迟，构造有数据依赖的指令链，迫使串行执行以暴露单条指令延迟。

## Microbenchmark 设计原则

每个 microbenchmark 都需要说明：

- 测什么
- 怎么测
- 为什么这样设计

实现方式不限制，C++ Kernel、Python 脚本等均可。核心原则：

1. 隔离性：一次只测一种操作，避免互相干扰。
2. 可控性：能精确控制操作规模，例如数据量、循环次数。
3. 可重复性：多次运行取平均或中位数，排除噪声。
4. 可验证性：结果应有合理物理意义，例如带宽不超过理论峰值。

## 必测参数清单

共 4 大类、21 个参数。每个参数都需要独立的 microbenchmark 测量，并在报告中提供测量方法、原始数据和分析。

### Scalar Unit

| 编号 | 参数 | 描述 | 单位 |
| --- | --- | --- | --- |
| S1 | 标量算术指令延迟 | 单条标量加法指令从发射到完成的周期数 | cycles |
| S2 | 标量单元吞吐率 | 单位时间内可完成的标量加法运算次数，考虑流水线深度 | ops/cycle |
| S3 | 标量访存延迟 | 标量 Load/Store 指令的访问延迟 | cycles |

### Vector Unit

| 编号 | 参数 | 描述 | 单位 |
| --- | --- | --- | --- |
| V1 | FP32 向量加法延迟 | 单条 FP32 向量加法指令从发射到完成的周期数 | cycles |
| V2 | FP32 向量乘法延迟 | 单条 FP32 向量乘法指令从发射到完成的周期数 | cycles |
| V3 | 向量单元吞吐率 | 单位时间内可完成的向量加法运算次数 | ops/cycle |
| V4 | 向量单元流水线深度 | 向量单元能同时容纳的处于不同阶段的指令数 | 条 |
| V5 | 向量寄存器访问延迟 | 向量寄存器读/写延迟，需要区分读写 | cycles |

### Cube Unit

| 编号 | 参数 | 描述 | 单位 |
| --- | --- | --- | --- |
| C1 | 矩阵乘延迟（单 tile） | 单个最小 tile，例如 16x16，矩阵乘法从发射到完成的周期数 | cycles |
| C2 | 矩阵乘吞吐率 | 单位时间内可完成的 MAC 操作数 | MAC/s 或 TFLOPS |
| C3 | 矩阵乘流水线深度 | Cube 单元支持的最大流水线并行度 | 条 |
| C4 | L0A/L0B/L0C 访问延迟 | Cube 专用缓冲区 L0A、L0B、L0C 的读写延迟 | cycles |
| C5 | 不同规模矩阵乘延迟缩放关系 | 矩阵规模 M/N/K 增大时矩阵乘延迟的增长规律，需要折线图 | - |

### MTE

| 编号 | 参数 | 描述 | 单位 |
| --- | --- | --- | --- |
| M1 | L1 Buffer 读取带宽 | 从 L1 Buffer 读取数据的峰值带宽 | GB/s |
| M2 | L1 Buffer 写入带宽 | 向 L1 Buffer 写入数据的峰值带宽 | GB/s |
| M3 | L0A 带宽 | L0A 缓冲区读写带宽 | GB/s |
| M4 | L0B 带宽 | L0B 缓冲区读写带宽 | GB/s |
| M5 | L0C 带宽 | L0C 缓冲区读写带宽 | GB/s |
| M6 | DDR/HBM 访存延迟 | 从片外内存读取数据的首字延迟 | cycles |
| M7 | 各级 Buffer 容量 | L1、L0A、L0B、L0C 各自的实际可用容量 | KB/MB |
| M8 | 数据搬运启动开销 | MTE 启动一次 DMA 传输的固定开销 | cycles |

## 评分标准

总分 100 分，由三部分组成。

### Microbenchmark 完成度：42 分

21 个参数，每个参数 2 分。每个参数需要提供一个独立可运行的 microbenchmark。

| 等级 | 分值 | 判定标准 |
| --- | --- | --- |
| 完整实现 | 2 分 | microbenchmark 代码可运行；测量方法合理，满足隔离性和可控性；产出测量结果并有原始数据 |
| 部分实现 | 1 分 | 有设计思路说明，但代码不可运行或测量方法有明显缺陷 |
| 未实现 / 仅文字描述 | 0 分 | 未提供 microbenchmark 代码，或仅有文字说明而无实际测量 |

PDF 明确建议优先完成核心参数：`S1`、`V1`、`V2`、`C1`、`M1`、`M2`，再逐步覆盖全部参数。

注意：完成度与准确度独立评分。即使测量结果不准确，只要 microbenchmark 设计合理且可运行，仍可获得完成度分数。

### 参数测量准确度：42 分

21 个参数，每个参数最高 2 分。实际值由助教统一标定。

| 等级 | 分值/参数 | 判定标准 |
| --- | --- | --- |
| A 级，精确 | 2 分 | 与实际值相对误差 <= 20% |
| B 级，良好 | 1.5 分 | 与实际值相对误差 <= 30% |
| C 级，可接受 | 0.5 分 | 与实际值相对误差 <= 50% |

### 报告质量：16 分

| 项目 | 分值 | 要求 |
| --- | --- | --- |
| microbenchmark 设计思路 | 6 分 | 每个 microbenchmark 有清晰设计说明，包括测什么、怎么测、为什么这样设计，逻辑合理 |
| 测量数据分析 | 6 分 | 有原始数据、统计信息，例如均值/方差、图表呈现，分析合理 |
| 报告结构与规范性 | 4 分 | 排版整洁，图表清晰 |

## 交付物要求

提交压缩包结构：

```text
学号_姓名/
├── report.pdf        # 实验报告，含 microbenchmark 设计思路、21 个参数测量结果、数据分析等
└── ubench/           # microbenchmark 实现，源代码，每个 microbenchmark 独立可运行
```

最终压缩为：

```text
学号_姓名.zip
```

PDF 首页另写提交文件名为：

```text
lab2_学号_姓名.zip
```

这里存在轻微命名不一致。后续打包时应优先按课程平台或教师最新要求确认；若没有额外说明，建议压缩包名使用 `lab2_学号_姓名.zip`，包内目录使用 `学号_姓名/`。

## 对后续实施最关键的结论

1. 这不是只写报告的作业，评分里 84 分直接来自 21 个参数的可运行 microbenchmark 和测量准确度。
2. 每个参数都要尽量独立可运行，不能只写一个大程序糊在一起，否则完成度评分风险很高。
3. 需要保留原始数据，并在报告里给统计结果和图表。后续项目结构应提前包含 `raw_data/`、`results/` 或类似目录，避免最后补数据。
4. 优先级应按 PDF 建议走：先做 `S1`、`V1`、`V2`、`C1`、`M1`、`M2`，这些是核心参数；再扩展到剩余 15 个。
5. 作业真正的难点不在“写 21 个文件”，而在昇腾平台上如何隔离硬件单元、获得稳定计时、把时间换算成 cycles/带宽/吞吐率，并保证结果不要离助教标定值太远。
